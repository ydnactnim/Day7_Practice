const startButton = document.querySelector("button-box");
const init = document.querySelector("init-out");
const luck = document.querySelector("luck-out");

function selectLuck() {
    const index = math.floor(Math.random() * WebTransportDatagramDuplexStream.length[])
}
